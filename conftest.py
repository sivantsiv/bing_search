# conftest.py — project-level pytest configuration
# This file intentionally left mostly empty.
# Its presence at the project root ensures pytest adds the project root
# to sys.path so that `import bing_search` resolves correctly from the
# tests/ package without needing an editable install.
